"""
Silly placeholder test for the template.
"""

from __future__ import annotations

from {{slug}}.{{slug}} import hello


def test_silly_placeholder() -> None:
    """
    Test for silly placeholder in
    {{slug}}.py.
    """
    want = "Hello {{slug}}"
    got = hello()

    assert got == want
